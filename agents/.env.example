# Copy to .env and fill in. .env is gitignored — never commit real keys.

# The model the judges think with. Any OpenAI-compatible endpoint: 0G, OpenRouter,
# Anthropic, OpenAI. Note that some gateways reject unrecognised clients, in which case
# this SDK cannot reach them at all.
#
# 0G ships two of these and they bill in completely different ways. Know which one you
# are pointing at, because the failure modes have nothing in common:
#
#   Router   https://router-api-testnet.integratenetwork.work/v1
#            key looks like sk-...  Free, and capped at 50 requests per DAY per ACCOUNT
#            (not per key: a second key shares the same allowance and dies with it) plus
#            10 per minute. Exposes /models. Resets at midnight UTC.
#
#   Direct   https://compute-network-<n>.integratenetwork.work/v1/proxy
#            key looks like app-sk-...  Minted by your wallet, no request cap at all.
#            Billed per token against a sub-account you prefunded for that one provider,
#            so it fails on an empty balance rather than on a call count. Does not serve
#            /models. Mint the key with:
#              0g-compute-cli inference get-secret --provider <provider address>
#            and see who is serving what with:
#              0g-compute-cli inference list-providers
#            Keep the model string matching what that provider advertises.
LLM_BASE_URL=https://router-api-testnet.integratenetwork.work/v1
LLM_API_KEY=sk-...
LLM_MODEL=qwen2.5-omni

# Optional second endpoint, used only when a primary call throws. Point it at a
# different provider: a fallback sharing an outage with the primary is not a fallback.
# Two Router keys are the worst case here, because they share one daily cap and go down
# together at exactly the moment you need the spare.
# It matters because a failed call becomes a safe pass, so an outage silently produces a
# round where every judge rejects everything, which reads as a verdict and is not one.
LLM_FALLBACK_BASE_URL=https://compute-network-6.integratenetwork.work/v1/proxy
LLM_FALLBACK_API_KEY=app-sk-...
LLM_FALLBACK_MODEL=qwen/qwen2.5-omni-7b

# Arc testnet
ARC_RPC_URL=https://rpc.testnet.arc.network

# Judge wallets. The orchestrator signs each investment with the deciding judge's own
# key. JUDGE_A is Alpha (also in contracts/.env); JUDGE_B/C are Nova/Sable.
JUDGE_A_PRIVATE_KEY=0x...
JUDGE_B_PRIVATE_KEY=0x...
JUDGE_C_PRIVATE_KEY=0x...

# Operator/deployer key. Runs the admin scripts (setup, close-loop): deposits capital,
# registers judges, and stands in as the paying customer for x402 revenue.
DEPLOYER_PRIVATE_KEY=0x...

# Optional knobs for the admin scripts (defaults shown).
# DEPOSIT_USDC=55        # capital added to the Fund in `bun run allocate`
# ALLOCATE_USDC=35       # USDC transferred into each judge's own wallet
# JUDGE_GAS_USDC=1       # gas top-up sent to each new judge wallet
# REVENUE_USDC=2         # simulated revenue per deal in `bun run close-loop`
# COHORT_SIZE=4          # pitches heard per round

# Startup wallets, for settling revenue back via RevenueShare in a later step.
# MeshRelay reuses the spike client wallet (CLIENT_PRIVATE_KEY from contracts/.env);
# the cold-start startups get their own throwaway keys.
CLIENT_PRIVATE_KEY=0x...
STARTUP_PIXELFORGE_PRIVATE_KEY=0x...
STARTUP_DATAORACLE_PRIVATE_KEY=0x...

# Circle Developer Controlled Wallets (Agent Stack). CIRCLE_API_KEY is a Sandbox
# Standard API key (format PREFIX:ID:SECRET). ENTITY_SECRET is the 32-byte hex secret
# you register once in the console; keep its recovery file safe, outside this repo.
CIRCLE_API_KEY=
ENTITY_SECRET=
