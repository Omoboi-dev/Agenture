[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

[rpc_endpoints]
arc = "https://rpc.testnet.arc.network"

[etherscan]
# Arc testnet explorer (arcscan). Verify flow to be confirmed in a later spike.
arc = { key = "empty", url = "https://testnet.arcscan.app/api", chain = 5042002 }
